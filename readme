Guia de Instalação e Uso – Vendas CLI
Este projeto fornece uma ferramenta de linha de comando (CLI) para análise de vendas a partir de arquivos CSV.
A seguir estão as instruções detalhadas para configurar o ambiente, instalar a CLI e executar o script.

🚀 1. Criar ambiente virtual
Para manter as dependências isoladas, crie um ambiente virtual:
# No Windows
py -m venv venv

# No Linux/Mac
python -m venv venv

Ative o ambiente virtual:
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate

📦 2. Instalar a CLI
Com o ambiente virtual ativo, instale o projeto em modo editable:
pip install -e .

Isso permite que você rode o comando vendas-cli diretamente no terminal sem precisar reinstalar a cada alteração no código.

▶️ 3. Executar o script
Agora você pode rodar o comando da CLI passando os parâmetros necessários:
vendas-cli --csv_file_path "docs/vendas_exemplo.csv" --format "text"

🔹 Parâmetros
- --csv_file_path → caminho para o arquivo CSV de vendas.
- --format → formato de saída (exemplo: text(tabela) ou json).

📌 Exemplo de uso
vendas-cli --csv_file_path "docs/vendas_exemplo.csv" --format "text"




